logging:
  level:
    root: INFO
management:
  endpoints:
    web:
      exposure:
        include: "*"
item-service-client:
  app-name: "anand-item-service"
rabbitmq:
  exchange-name: "eventExchange"
  queue-name: "orderServiceQueue"
  routing-key: "customer.created"
app:
  custom:
    message: "Hello Service3 User!"