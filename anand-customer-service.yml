logging:
  level:
    root: INFO
management:
  endpoints:
    web:
      exposure:
        include: "*"
rabbitmq:
  exchange-name: "eventExchange"
  routing-key: "customer.created"
app:
  custom:
    message: "Hello Service1 User!"